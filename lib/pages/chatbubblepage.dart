import 'package:flutter/material.dart';

class ChatBubblePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Hello Chat page!')
      ,
    );
  }
}
